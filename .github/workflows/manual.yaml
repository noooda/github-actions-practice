name: Manual

on:
  workflow_dispatch:
    inputs:
      greeting:
        type: choice 
        options:
          - Hello
          - Hi
          - Good Afternoon
        required: true
        description: A cheerful word

env:
  BRANCH: main
  ANIMAL: ${{vars.ANIMAL}}
  IMPORTANT_PASSWORD: ${{secrets.IMPORTANT_PASSWORD}}

jobs:
  printenv:
    runs-on: ubuntu-latest
    env:
      ACTOR: ${{github.actor}}
    steps:
      - run: echo "$BRANCH"
      - run: echo "$ACTOR"
      - run: echo "$ANIMAL"
      - run: echo "$IMPORTANT_PASSWORD"
      - run: echo "${{inputs.greeting}}"
      - rnn: echo "${{`Right!!!!!!!!!!`}}"
        if: ${{10 == 10}}
